#!/bin/bash
# Скрипт для развертывания бота на сервере

echo "🚀 Развертывание Telegram бота для подсчета кассы..."

# Проверка наличия Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python3 не найден. Установите Python3."
    exit 1
fi

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активация виртуального окружения
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Установка зависимостей
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Проверка наличия .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден!"
    echo "Создайте файл .env с содержимым:"
    echo "BOT_TOKEN=your_bot_token_here"
    exit 1
fi

# Создание директории для логов
mkdir -p logs

echo "✅ Развертывание завершено!"
echo ""
echo "Для запуска бота используйте:"
echo "  ./start.sh          - запуск в фоне"
echo "  ./stop.sh           - остановка"
echo "  ./restart.sh        - перезапуск"
echo "  ./status.sh         - статус"
echo ""
echo "Или используйте systemd:"
echo "  sudo ./install_service.sh - установка как службы"

