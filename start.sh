#!/bin/bash
# Запуск бота в фоновом режиме

cd "$(dirname "$0")"

# Проверка и создание виртуального окружения
if [ ! -f "venv/bin/activate" ]; then
    echo "⚠️  Виртуальное окружение не найдено. Создание..."
    if ! command -v python3 &> /dev/null; then
        echo "❌ Python3 не найден. Установите Python3."
        exit 1
    fi
    
    python3 -m venv venv
    
    if [ ! -f "venv/bin/activate" ]; then
        echo "❌ Не удалось создать виртуальное окружение!"
        exit 1
    fi
    
    echo "📦 Установка зависимостей..."
    source venv/bin/activate
    pip install --upgrade pip
    pip install -r requirements.txt
else
    # Активация существующего виртуального окружения
    source venv/bin/activate
fi

# Проверка наличия .env
if [ ! -f ".env" ]; then
    echo "❌ Файл .env не найден!"
    exit 1
fi

# Создание директории для логов
mkdir -p logs

# Запуск бота в фоне с использованием виртуального окружения
echo "🚀 Запуск бота..."
# Используем python из venv для гарантии правильного окружения
nohup venv/bin/python3 main.py > logs/bot.log 2>&1 &
echo $! > bot.pid

echo "✅ Бот запущен! PID: $(cat bot.pid)"
echo "📋 Логи: tail -f logs/bot.log"

